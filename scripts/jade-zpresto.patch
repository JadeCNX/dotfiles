diff --git a/runcoms/zpreztorc b/runcoms/zpreztorc
index 28b6005..b6ac522 100644
--- a/runcoms/zpreztorc
+++ b/runcoms/zpreztorc
@@ -34,7 +34,16 @@ zstyle ':prezto:load' pmodule \
   'directory' \
   'spectrum' \
   'utility' \
+  'ssh' \
   'completion' \
+  'homebrew' \
+  'osx' \
+  'node' \
+  'python' \
+  'git' \
+  'syntax-highlighting' \
+  'history-substring-search' \
+  'tmux' \
   'prompt'
 
 #
@@ -57,7 +66,7 @@ zstyle ':prezto:load' pmodule \
 #
 
 # Set the key mapping style to 'emacs' or 'vi'.
-zstyle ':prezto:module:editor' key-bindings 'emacs'
+# zstyle ':prezto:module:editor' key-bindings 'vi'
 
 # Auto convert .... to ../..
 # zstyle ':prezto:module:editor' dot-expansion 'yes'
@@ -97,7 +106,7 @@ zstyle ':prezto:module:editor' key-bindings 'emacs'
 #
 
 # Set the keyword used by `mand` to open man pages in Dash.app
-# zstyle ':prezto:module:osx:man' dash-keyword 'manpages'
+zstyle ':prezto:module:osx:man' dash-keyword 'manpages'
 
 #
 # Pacman
@@ -165,13 +174,13 @@ zstyle ':prezto:module:prompt' theme 'sorin'
 # Set syntax highlighters.
 # By default, only the main highlighter is enabled.
 # zstyle ':prezto:module:syntax-highlighting' highlighters \
-#   'main' \
-#   'brackets' \
-#   'pattern' \
-#   'line' \
-#   'cursor' \
-#   'root'
-#
+  # 'main' \
+  # 'brackets' \
+  # 'pattern' \
+  # 'line' \
+  # 'cursor' \
+  # 'root'
+
 # Set syntax highlighting styles.
 # zstyle ':prezto:module:syntax-highlighting' styles \
 #   'builtin' 'bg=blue' \
diff --git a/runcoms/zprofile b/runcoms/zprofile
index acd7742..73966ea 100644
--- a/runcoms/zprofile
+++ b/runcoms/zprofile
@@ -17,8 +17,8 @@ fi
 # Editors
 #
 
-export EDITOR='nano'
-export VISUAL='nano'
+export EDITOR='vim'
+export VISUAL='code'
 export PAGER='less'
 
 #
@@ -29,6 +29,7 @@ if [[ -z "$LANG" ]]; then
   export LANG='en_US.UTF-8'
 fi
 
+
 #
 # Paths
 #
@@ -36,6 +37,12 @@ fi
 # Ensure path arrays do not contain duplicates.
 typeset -gU cdpath fpath mailpath path
 
+# export GOPATH=/Users/jade/Develops/Go
+# export GOROOT=/usr/local/opt/go/libexec
+export ROYALE_HOME=/Users/jade/Develops/apache-royale-compiler-0.9.2-src
+export FLEX_HOME=/Users/jade/Develops/litic/sdk4.15
+# export TC_DEPLOY_DIR=/Users/jade/Develops/litic/gui_bin
+
 # Set the list of directories that cd searches.
 # cdpath=(
 #   $cdpath
@@ -44,9 +51,16 @@ typeset -gU cdpath fpath mailpath path
 # Set the list of directories that Zsh searches for programs.
 path=(
   /usr/local/{bin,sbin}
+  # $GOPATH/bin
+  # $GOROOT/bin
   $path
 )
 
+
+# Delay transition between modes (n * 0.1 sec.)
+export KEYTIMEOUT=1
+
+
 #
 # Less
 #
@@ -54,10 +68,41 @@ path=(
 # Set the default Less options.
 # Mouse-wheel scrolling has been disabled by -X (disable screen clearing).
 # Remove -X and -F (exit if the content fits on one screen) to enable it.
-export LESS='-F -g -i -M -R -S -w -X -z-4'
+export LESS='-g -i -M -R -S -w -z-4'
 
 # Set the Less input preprocessor.
 # Try both `lesspipe` and `lesspipe.sh` as either might exist on a system.
 if (( $#commands[(i)lesspipe(|.sh)] )); then
   export LESSOPEN="| /usr/bin/env $commands[(i)lesspipe(|.sh)] %s 2>&-"
 fi
+
+export LC_CTYPE=en_US.UTF-8
+export LC_ALL=en_US.UTF-8
+
+# export PYTHONPATH="/usr/local/lib/python2.7/site-packages:$PYTHONPATH"
+# export PYTHONPATH="/usr/local/Cellar/python/2.7.14/Frameworks/Python.framework/Versions/2.7/lib/python2.7/site-packages:$PYTHONPATH"
+# /usr/local/Cellar/python/2.7.14_3/Frameworks/Python.framework/Versions/2.7/lib/python2.7/site-packages/
+
+
+
+#
+# FZF
+#
+export FZF_DEFAULT_COMMAND='rg --files --no-ignore --hidden --follow --glob "!.git/*"'
+
+alias vim='nvim'
+alias ls='ls -Fh'
+alias showhidden='defaults write com.apple.finder AppleShowAllFiles -bool'
+
+alias git-show-tags='git tag --sort=objecttype --sort=-creatordate | git show --date=local --pretty="format:%C(auto)%h%C(green)%d %C(blue)%an %C(auto)%s %x1b[2m%ad%x1b[0m%Creset" -s --stdin'
+#alias git-not-release='git cherry origin/release origin/develop | perl -ne "m/^[\+]/&&s/^..//&&print" | git show --pretty="format:%C(auto)%h %s %d%an, %ai%Creset, %ai%Creset" -s --author=jade --stdin'
+alias git-not-release='~/scripts/cherry_release.py | git show --pretty="format:%C(auto)%h%C(green)%d %C(blue)%an %C(auto)%s %x1b[2m%ad%x1b[0m%Creset" -s --author=jade --stdin'
+#alias swfconfilctcheckout='git pull | grep "\t.*swf$" | xargs git checkout | xargs echo "checkout conflict success\n"'
+# alias git-pull-ignore-swf='~/Develops/scripts/._gcp.py | xargs -I % sh -c "git checkout % ; echo checkout %" && git pull && git fat pull'
+alias git-select-cherry='~/Develops/scripts/git-select-cherry.py'
+
+alias tail-flash='less +F /Users/jade/Library/Preferences/Macromedia/Flash\ Player/Logs/flashlog.txt'
+# alias tail-flash-grep='tail -100 -f /Users/jade/Library/Preferences/Macromedia/Flash\ Player/Logs/flashlog.txt | grep --line-buffered'
+
+alias tail-flash-chrome='less +F /Users/jade/Library/Application\ Support/Google/Chrome/Default/Pepper\ Data/Shockwave\ Flash/WritableRoot/Logs/flashlog.txt'
+# alias tail-flash-chrome-grep='tail -100 -f /Users/jade/Library/Application\ Support/Google/Chrome/Default/Pepper\ Data/Shockwave\ Flash/WritableRoot/Logs/flashlog.txt | grep --line-buffered'
diff --git a/runcoms/zshrc b/runcoms/zshrc
index 039b882..cc26c6b 100644
--- a/runcoms/zshrc
+++ b/runcoms/zshrc
@@ -11,3 +11,8 @@ if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
 fi
 
 # Customize to your needs...
+
+test -e "${HOME}/.iterm2_shell_integration.zsh" && source "${HOME}/.iterm2_shell_integration.zsh"
+
+
+[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
